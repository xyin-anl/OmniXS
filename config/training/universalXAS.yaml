defaults:
  - defaults
  - _self_

name: universalXAS
element: All # placeholder for hparms
type: FEFF # FEFF, VASP ..
feature: m3gnet # m3gnet, ACSF...

ml_splits:
  _target_: refactor.data.merge_ml_splits.FEFFSplits

module:
  _target_: refactor.model.training.PlModule
  model: ${model}
